/* parser */
module.exports = (function () {

  var util = require("util"),

  input  = null,
  mixins = [],
  nodes  = null;

  function parse(lex, options) {

    input = lex;

    

    return nodes;
  }


  return {
    parse: parse
  }

}());